
public class TestOrder extends Order{
	public void printitems( String itemname, int itemid, int amount)
	{
		
	}
	
public static void main(String args[])
{
	Order t = new Order();
	t.total();
	t.getorderdate();
	t.printItems(itemname, itemID);
	t.printItems(itemname, itemID, amount);
	printItems(itemname, itemID, amount);
}
}
